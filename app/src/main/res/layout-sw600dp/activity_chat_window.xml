<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.markg.androidlabs.ChatWindow">

    <ListView
        android:id="@+id/chatListView"
        android:layout_width="300dp"
        android:layout_height="425dp"
        android:layout_above="@+id/sendButton"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"></ListView>

    <EditText
        android:id="@+id/chatEditText"
        android:layout_width="270dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/sendButton" />

    <Button
        android:id="@+id/sendButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/chatEditText"
        android:layout_alignBottom="@+id/chatEditText"
        android:layout_alignEnd="@+id/chatListView"
        android:text="Send" />

    <FrameLayout
        android:id="@+id/messageDetailFrame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="20dp"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/chatListView">

    </FrameLayout>

</RelativeLayout>
